# ZULF Lab dependencies.
#
#   pip install -r requirements.txt
#   pytest -q                      # 79 tests
#
# The simulator and forward model need only the first three. torch/sbi/dynesty
# are needed for the inference layer (zulf_infer.py, sbc_check.py,
# nested_reference.py) and pypdf only for make_figure1.py, which recovers a
# published spectrum from a paper's vector art.

numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# inference stack
torch>=2.0
sbi>=0.27
dynesty>=3.0

# reading published figures
pypdf>=4.0

# tests
pytest>=7.0
